import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Theme from "contexts/themeContext";
import Header from "components-themed/Header";
import { ReactComponent as Logo } from "assets/ons-logo.svg";
import exampleList from "./ExampleList";


<Meta title="Themed components/ONS/Header" component={Header} />

# Header (ONS theme)

## Internal

<Canvas>
    <Story name="Internal">
        <Theme themeName={"ons"}>
            <Header variant="Internal" logo={<Logo/>}>Service title</Header>
        </Theme>
    </Story>
</Canvas>

## Internal with description/tagline

<Canvas>
    <Story name="Internal with description/tagline">
        <Theme themeName={"ons"}>
            <Header variant="Internal" 
                    withDescription 
                    headerDescription="This is the description or tagline"
                    logo = {<Logo/>}>
            Service title</Header>
        </Theme>
    </Story>
</Canvas>

## Internal with list

<Canvas>
    <Story name="Internal with list">
        <Theme themeName={"ons"}>
            <Header variant="Internal" 
                    withDescription 
                    logo = {<Logo/>}
                    exampleList={exampleList}>
            Service title
            </Header>
        </Theme>
    </Story>
</Canvas>


## External

<Canvas>
    <Story name="External">
        <Theme themeName={"ons"}>
            <Header variant="External" logo={<Logo/>}>Service title</Header>
        </Theme>
    </Story>
</Canvas>

## External with description/tagline

<Canvas>
    <Story name="External with description/tagline">
        <Theme themeName={"ons"}>
            <Header variant="External" 
                    withDescription 
                    headerDescription="This is the description or tagline" 
                    logo={<Logo/>}> 
            Service title</Header>
        </Theme>
    </Story>
</Canvas>