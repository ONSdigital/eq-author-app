mutation CreateList {
  createList {
    id
    lists {
      id
      listName
      displayName
      answers {
        id
      }
      validationErrorInfo {
        id
        errors {
          id
          type
          field
          errorCode
        }
        totalCount
      }
    }
  }
}
