mutation createQuestionnaire($input: CreateQuestionnaireInput!) {
  createQuestionnaire(input: $input) {
    id
    title
    createdAt
    createdBy {
      name
    }
    sections {
      id
      pages {
        id
      }
    }
  }
}
