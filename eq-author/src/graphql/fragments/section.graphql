fragment Section on Section {
  id
  title
  alias
  introductionTitle
  introductionContent
  questionnaire {
    id
    navigation
    hub
  }
  validationErrorInfo {
    id
    errors {
      id
      type
      field
      errorCode
    }
    totalCount
  }
}
