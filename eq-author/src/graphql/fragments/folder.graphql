#import "graphql/fragments/validationErrorInfo.graphql"

fragment Folder on Folder {
  id
  alias
  displayName
  enabled
  position
  validationErrorInfo {
    ...ValidationErrorInfo
  }
  pages {
    id
  }
}
