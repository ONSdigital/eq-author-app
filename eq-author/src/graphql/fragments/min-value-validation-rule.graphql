fragment MinValueValidationRule on MinValueValidationRule {
  id
  custom
  inclusive
  entityType
  previousAnswer {
    id
    displayName
  }
  validationErrorInfo {
    id
    errors {
      id
      type
      field
      errorCode
    }
    totalCount
  }
}
