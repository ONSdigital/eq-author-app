fragment TotalValidationRule on TotalValidationRule {
  id
  enabled
  entityType
  custom
  previousAnswer {
    id
    displayName
  }
  condition
  validationErrorInfo {
    id
    errors {
      id
      type
      field
      errorCode
    }
    totalCount
  }
}
