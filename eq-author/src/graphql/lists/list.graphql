#import "../fragments/validationErrorInfo.graphql"
#import "./listAnswer.graphql"

fragment List on List {
  id
  listName
  displayName
  answers {
    ...ListAnswer
  }
  validationErrorInfo {
    ...ValidationErrorInfo
  }
  metadata {
    id
    displayName
    key
    type
  }
}