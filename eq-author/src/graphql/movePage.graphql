mutation MovePage($input: MovePageInput!) {
  movePage(input: $input) {
    position
    id
    title
    folder {
      id
    }
    section {
      id
      folders {
        id
        position
        pages {
          id
          position
        }
      }
    }
  }
}
