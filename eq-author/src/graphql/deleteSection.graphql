mutation DeleteSection($input: DeleteSectionInput!) {
  deleteSection(input: $input) {
    id
    sections {
      id
      folders {
        id
      }
    }
    questionnaireInfo {
      totalSectionCount
    }
  }
}
