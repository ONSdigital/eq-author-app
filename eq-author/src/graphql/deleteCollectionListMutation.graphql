mutation DeleteList($input: DeleteListInput) {
  deleteList(input: $input) {
    lists {
      id
      listName
      displayName
      answers {
        id
      }
      validationErrorInfo {
        id
        errors {
          id
          type
          field
          errorCode
        }
        totalCount
      }
    }
  }
}
