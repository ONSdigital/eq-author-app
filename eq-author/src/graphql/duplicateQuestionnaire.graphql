mutation duplicateQuestionnaire($input: DuplicateQuestionnaireInput!) {
  duplicateQuestionnaire(input: $input) {
    id
    displayName
    createdAt
    createdBy {
      id
      name
    }
    sections {
      id
      pages {
        id
      }
    }
    introduction {
      id
    }
  }
}
