mutation MoveFolderMutation($input: MoveFolderInput!) {
  moveFolder(input: $input) {
    id
    alias
    displayName
    enabled
    pages {
      id
      title
      position
    }
    position
  }
}
