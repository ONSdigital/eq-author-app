mutation DeleteFolder($input: DeleteFolderInput!) {
  deleteFolder(input: $input) {
    id
    sections {
      id
      folders {
        id
        enabled
        pages {
          id
        }
      }
    }
  }
}
