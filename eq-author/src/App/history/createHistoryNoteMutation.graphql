mutation CreateHistoryNote($input: createHistoryNoteInput!) {
  createHistoryNote(input: $input) {
    id
    publishStatus
    questionnaireTitle
    bodyText
    user {
      id
      displayName
      email
      name
    }
    time
  }
}
