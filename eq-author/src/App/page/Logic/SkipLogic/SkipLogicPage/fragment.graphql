#import "./SkipLogicEditor/fragment.graphql"

fragment SkipLogicPage on QuestionPage {
  id
  skipConditions {
    ...SkipLogicEditor
  }
  validationErrorInfo {
    id
    errors {
      id
      type
      field
      errorCode
    }
    totalCount
  }
}
