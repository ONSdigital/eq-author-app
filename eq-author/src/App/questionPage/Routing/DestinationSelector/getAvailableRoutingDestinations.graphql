query GetAvailableRoutingDestinations($input: QueryInput!) {
  page(input: $input) {
    id
    availableRoutingDestinations {
      logicalDestinations {
        id
        logicalDestination
      }
      pages {
        id
        displayName
      }
      sections {
        id
        displayName
      }
    }
  }
}
