# import "./RuleEditor/fragment.graphql"

fragment RoutingEditor on Routing2 {
  id
  rules {
    ...RuleEditor
  }
  else {
    logical
    page {
      id
      displayName
    }
    section {
      id
      displayName
    }
  }
}
