mutation UpdateSubmission($input: UpdateSubmissionInput!) {
  updateSubmission(input: $input) {
    id
    furtherContent
    viewPrintAnswers
    feedback
    validationErrorInfo {
      id
      errors {
        id
        type
        field
        errorCode
      }
      totalCount
    }
  }
}
