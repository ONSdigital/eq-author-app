query GetQuestionnaire($input: QueryInput!) {
  questionnaire(input: $input) @client {
    id
    isPublic
    createdBy {
      id
      name
      email
    }
    editors {
      id
      name
      email
    }
  }
}
