#import "./listAnswer.graphql"

query Lists {
  lists {
    id
    listName
    displayName
    answers {
      ...ListAnswer
    }
    validationErrorInfo {
      id
      errors {
        id
        type
        field
        errorCode
      }
      totalCount
    }
  }
}
