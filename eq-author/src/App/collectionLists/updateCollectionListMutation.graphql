#import "./listAnswer.graphql"

mutation UpdateList($input: UpdateListInput) {
  updateList(input: $input) {
    id
    listName
    displayName
    answers {
      ...ListAnswer
    }
    validationErrorInfo {
      id
      errors {
        id
        type
        field
        errorCode
      }
      totalCount
    }
  }
}
