#import "./listAnswer.graphql"

mutation CreateList {
  createList {
    lists {
      id
      listName
      displayName
      answers {
        ...ListAnswer
      }
      validationErrorInfo {
        id
        errors {
          id
          type
          field
          errorCode
        }
        totalCount
      }
    }
  }
}
