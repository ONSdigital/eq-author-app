#import "./NewCommentFragmentSection.graphql"
#import "./NewCommentFragmentPage.graphql"

mutation deleteComment($input: DeleteCommentInput!) {
  deleteComment(input: $input) {
    section {
      ...NewCommentFragmentSection
    }
    page {
      ...NewCommentFragmentPage
    }

    # __typename
    # ... on QuestionPage {
    #   id
    #   comments {
    #     id
    #   }
    # }
    # ... on Section {
    #   id
    #   comments {
    #     id
    #   }
    # }
  }
}
