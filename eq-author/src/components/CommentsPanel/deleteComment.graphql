mutation deleteComment($input: DeleteCommentInput!) {
  deleteComment(input: $input) {
    ... on Page {
      id
      comments {
        id
      }
    }
    ... on Section {
      id
      comments {
        id
      }
    }
  }
}
