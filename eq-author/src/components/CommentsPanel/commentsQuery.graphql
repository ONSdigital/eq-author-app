#import "./commentFragment.graphql"

query CommentsQuery($input: QueryInput!) {
  section(input: $input) {
    id
    comments {
      ...CommentFragment
    }
  }
  page(input: $input) {
    id
    comments {
      ...CommentFragment
    }
  }
}
