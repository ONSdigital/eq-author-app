coverage:
  status:
    project:
      author:
        flags: author
      api:
        flags: api
      publisher:
        flags: publisher
  patch: false

flags:
  author:
    paths:
      - eq-author/
  api:
    paths:
      - eq-author-api/
  publisher:
    paths:
      - eq-publisher/
# comment: off
comment:
layout: diff, files # accepted in any order: reach, diff, flags, and/or files
