const { createList } = require("./createList");
const { updateList } = require("./updateList");
const { deleteList } = require("./deleteList");
const { createListAnswer } = require("./createListAnswer");
const { updateListAnswer } = require("./updateListAnswer");
const { deleteListAnswer } = require("./deleteListAnswer");

module.exports = {
  createList,
  updateList,
  deleteList,
  createListAnswer,
  updateListAnswer,
  deleteListAnswer,
};
