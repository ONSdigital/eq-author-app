const { getValidationQuery, queryValidation } = require("./queryValidation");
const {
  toggleValidation,
  toggleValidationMutation,
} = require("./toggleValidation");
const {
  updateValidation,
  updateValidationMutation,
} = require("./updateValidation");

module.exports = {
  getValidationQuery,
  queryValidation,
  toggleValidation,
  toggleValidationMutation,
  updateValidation,
  updateValidationMutation,
};
